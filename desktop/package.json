{
  "name": "school-manager-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "Windows desktop shell for the School Manager web app (Tauri).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build --bundles msi",
    "icons": "tauri icon app-icon.svg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
